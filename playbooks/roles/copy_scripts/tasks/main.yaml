---
- name: Copy required scripts
  hosts: localhost
  gather_facts: False
  remote_user: ubuntu
  become: True
  vars:
    ansible_host_key_checking: false
  tasks:
    - name: Copy controller script
      copy:
        src: "{{ ansible_env.HOME }}/openstack-helm-deployment/scripts/"
        dest: scripts/
        mode: 0755
        group: ubuntu
        owner: ubuntu
      delegate_to: "{{ item }}"
      delegate_facts: True
      loop: "{{ groups['k8s_contr'] }}"

    - name: Copy upgrade host script to minion
      copy:
        src: "{{ ansible_env.HOME }}/openstack-helm-deployment/scripts/upgrade-host.sh"
        dest: scripts/
        mode: 0755
        group: ubuntu
        owner: ubuntu
      delegate_to: "{{ item }}"
      delegate_facts: True
      loop: "{{ groups['k8s_minion'] }}"

    - name: Copy setup new host script to minion
      copy:
        src: "{{ ansible_env.HOME }}/openstack-helm-deployment/scripts/setup-new-host.sh"
        dest: scripts/
        mode: 0755
        group: ubuntu
        owner: ubuntu
      delegate_to: "{{ item }}"
      delegate_facts: True
      loop: "{{ groups['k8s_minion'] }}"

    - name: Copy setup new host script to minion
      copy:
        src: "{{ ansible_env.HOME }}/openstack-helm-deployment/scripts/install-kubernetes.sh"
        dest: scripts/
        mode: 0755
        group: ubuntu
        owner: ubuntu
      delegate_to: "{{ item }}"
      delegate_facts: True
      loop: "{{ groups['k8s_minion'] }}"

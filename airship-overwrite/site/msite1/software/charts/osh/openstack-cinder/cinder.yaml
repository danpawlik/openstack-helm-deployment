---
schema: armada/Chart/v1
metadata:
  schema: metadata/Document/v1
  name: msite1-cinder-airskiff
  layeringDefinition:
    abstract: false
    layer: site
    parentSelector:
      name: cinder-global
    actions:
      - method: merge
        path: .
  storagePolicy: cleartext
data:
  chart_name: msite1-cinder
  release: msite1-cinder
  namespace: msite1
  wait:
    timeout: 300
    labels:
      release_group: airship-msite1-cinder
  install:
    no_hooks: false
  upgrade:
    no_hooks: false
    pre:
      delete:
        - type: job
          labels:
            release_group: airship-msite1-cinder
...

---
schema: armada/Chart/v1
metadata:
  schema: metadata/Document/v1
  name: msite1-memcached-airskiff
  layeringDefinition:
    abstract: false
    layer: site
    parentSelector:
      name: openstack-memcached-global
      component: memcached
    actions:
      - method: merge
        path: .
  storagePolicy: cleartext
data:
  namespace: msite1
  release: msite1-memcached
  chart_name: msite1-memcached
  wait:
    timeout: 300
    labels:
      release_group: airship-msite1-memcached
  install:
    no_hooks: false
  upgrade:
    no_hooks: false
    pre:
      delete:
        - type: job
          labels:
            release_group: airship-msite1-memcached
...
